# ================================
# Enterprise AI Orchestrator
# ================================

# Server
API_PORT=50000
RATE_LIMIT_PER_MINUTE=20
SESSION_EXPIRY=30m
CHAT_TIMEOUT=120s
RAG_INGEST_TIMEOUT=120s
AUDIT_TIMEOUT=5s
HEALTH_TIMEOUT=5s
MIGRATION_TIMEOUT=30s
MAX_UPLOAD_MB=10

# LLM Backend
# ollama = PoC (M1/Local)
# vllm   = Production (NVIDIA)
LLM_BACKEND=ollama
LLM_HOST=host.docker.internal
LLM_PORT=11434
LLM_MODEL=qwen2.5:7b

# Embedding
EMBED_MODEL=nomic-embed-text-v2-moe

# Database
DB_HOST=orchestrator-postgres
DB_PORT=5432
DB_NAME=orchestrator
DB_USER=orchestrator
# Development default. Production must use a real secret.
DB_PASS=changeme

# Active Directory
AD_SERVER=192.168.x.x
AD_PORT=389
AD_BASE_DN=DC=your-domain,DC=com
AD_DOMAIN=your-domain.com

# JWT
# Development placeholder. Production must use a strong random value.
JWT_SECRET=change-this-to-random-string-in-production
JWT_EXPIRY=8h

# Dev Mode (ข้าม AD — ใช้เฉพาะ development เท่านั้น)
DEV_MODE=true
DEV_USERNAME=dev
DEV_PASSWORD=dev1234

# System Prompt
SYSTEM_PROMPT=You are a helpful enterprise AI assistant. You must always respond in Thai language only. Never use Chinese, English, or any other language. Thai language only.
